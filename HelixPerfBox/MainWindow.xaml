<Window x:Class="HelixPerfBox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:h="http://helix-toolkit.org/wpf"
        xmlns:helixPerfBox="clr-namespace:HelixPerfBox"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="MainWindow"
        Width="525"
        Height="350"
        d:DataContext="{d:DesignInstance helixPerfBox:ViewModel}"
        mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <!--
                <Button x:Name="Button"
                Click="OnAdd100Click"
                Content="Add" />
                <ToggleButton Click="OnVisibility" Content="Visible" />
            -->
            <Button Click="OnZoomExtentsClick" Content="ZoomExtents" />
        </StackPanel>

        <h:HelixViewport3D x:Name="Viewport3D"
                           Grid.Row="1"
                           ZoomExtentsWhenLoaded="True">
            <h:SunLight />
            <helixPerfBox:DemoElement3D/>
            <helixPerfBox:Selector3D ItemsSource="{Binding RedBalls}" />
            <helixPerfBox:Selector3D ItemsSource="{Binding BlueBalls}" />
        </h:HelixViewport3D>
    </Grid>
</Window>
