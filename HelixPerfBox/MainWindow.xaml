<Window x:Class="HelixPerfBox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:h="http://helix-toolkit.org/wpf"
        xmlns:helixPerfBox="clr-namespace:HelixPerfBox"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="MainWindow"
        Width="525"
        Height="350"
        d:DataContext="{d:DesignInstance helixPerfBox:ViewModel}"
        mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">

            <ToggleButton Content="Visible" IsChecked="{Binding IsBallsVisible}" />

            <ComboBox x:Name="MaterialBox" SelectedIndex="0">
                <ComboBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type Material}">
                        <Ellipse Width="20"
                                 Height="20"
                                 Fill="{Binding}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Click="OnZoomExtentsClick" Content="ZoomExtents" />
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="30" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <DataGrid ItemsSource="{Binding RedBalls}"
                      SelectedItem="{Binding SelectedRed}"
                      SelectionMode="Single"
                      SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Point3D}" Header="Point" />
                </DataGrid.Columns>
                <!--
                    <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type helixPerfBox:Ball}">
                    <TextBlock Text="{Binding Point3D}" />
                    </DataTemplate>
                    </ListBox.ItemTemplate>
                -->
            </DataGrid>
            <h:HelixViewport3D x:Name="Viewport3D"
                               Grid.Column="1"
                               ZoomExtentsWhenLoaded="True">
                <h:HelixViewport3D.Resources>
                    <!--
                        <DataTemplate x:Key="BallTemplate" DataType="{x:Type helixPerfBox:Ball}">
                        <FrameworkContentElement>
                        <h:SphereVisual3D Center="{Binding Point3D}" Radius="{Binding Radius}" />
                        </FrameworkContentElement>
                        </DataTemplate>
                    -->
                    <helixPerfBox:TemplateModel x:Key="BallTemplate" DataType="{x:Type helixPerfBox:Ball}">
                        <h:SphereVisual3D Center="{Binding Point3D}" Radius="{Binding Radius}" />
                    </helixPerfBox:TemplateModel>
                </h:HelixViewport3D.Resources>
                <h:SunLight />
                <!--  <helixPerfBox:Selector3D ItemsSource="{Binding RedBalls}" SelectedItem="{Binding SelectedRed}" />  -->
                <helixPerfBox:Selector3D ItemsSource="{Binding RedBalls}" SelectedItem="{Binding SelectedRed}" />

                <helixPerfBox:ItemsControl3D ItemTemplate="{StaticResource BallTemplate}" ItemsSource="{Binding BlueBalls}" />
            </h:HelixViewport3D>
        </Grid>
    </Grid>
</Window>
