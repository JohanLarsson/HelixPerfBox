<Window x:Class="HelixPerfBox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:h="http://helix-toolkit.org/wpf"
        xmlns:helixPerfBox="clr-namespace:HelixPerfBox"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="MainWindow"
        Width="525"
        Height="350"
        d:DataContext="{d:DesignInstance helixPerfBox:ViewModel}"
        mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">

            <ToggleButton IsChecked="{Binding IsBallsVisible}" Content="Visible" />

            <ComboBox x:Name="MaterialBox" SelectedIndex="0">
                <ComboBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type Material}">
                        <Ellipse Width="20"
                                 Height="20"
                                 Fill="{Binding}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Click="OnZoomExtentsClick" Content="ZoomExtents" />
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="30"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <ListBox ItemsSource="{Binding RedBalls}" SelectedItem="{Binding SelectedRed}">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type helixPerfBox:Ball}">
                        <TextBlock Text="{Binding Point3D}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <h:HelixViewport3D x:Name="Viewport3D"
                               Grid.Column="1"
                               ZoomExtentsWhenLoaded="True">
                <h:SunLight />
                <!--  <helixPerfBox:Selector3D ItemsSource="{Binding RedBalls}" SelectedItem="{Binding SelectedRed}" />  -->
                <!--<helixPerfBox:Selector3D ItemsSource="{Binding RedBalls}" SelectedItem="{Binding SelectedRed}" />-->
                <helixPerfBox:Balls3D ItemsSource="{Binding BlueBalls}" Material="{Binding SelectedItem, ElementName=MaterialBox}" />
            </h:HelixViewport3D>
        </Grid>
    </Grid>
</Window>
